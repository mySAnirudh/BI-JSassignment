// 4. Assigning the Value of One Variable to Another :
// After a value is assigned to a variable using the assignment operator, you can
// assign the value of that variable to another variable using the assignment operator.
// var myVar;
// myVar = 5;
// var myNum;
// myNum = myVar;
// The above declares a myVar variable with no value, then assigns it the value 5.

// Next, a variable named myNum is declared with no value. Then, the contents of
// myVar (which is 5) is assigned to the variable myNum. Now, myNum also has the
// value of 5.
// Assign the contents of a to variable b.

// assert(/var a;/.test(code) && /a = 7;/.test(code) && /var b;/.test(code));
// b should have a value of 7.

// assert(typeof b === 'number' && b === 7);
// a should be assigned to b with =.

// assert(/b\s*=\s*a\s*/g.test(code));



// if (typeof a != 'undefined') {
//     a = undefined;
//   }
//   if (typeof b != 'undefined') {
//     b = undefined;
//   }


//   (function(a, b) {
//     return 'a = ' + a + ', b = ' + b;
//   })(a, b);

//   (function(a, b) {
//     return 'a = ' + a + ', b = ' + b;
//   })(a, b);

  
  // Setup
var a;
a = 7;
var b;

// Only change code below this line

//Answer:- 

var a;
a = 7;
var b;
b = a;
